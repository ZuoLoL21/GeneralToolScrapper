ALWAYS read the documentation (README.md and the markdowns in docs/) 
before exploring the codebase. They are normally more than enough
to represent the codebase

Assume everything is properly implemented, you do not need to look at files
that are not used directly

When implementing a feature, add a main() to each main code file to run an
example 